{
  "name": "tiktok-live-event-server",
  "version": "0.1.0",
  "private": true,
  "description": "TikTok LIVE のギフト / フォロー / いいねをリアルタイムに受信してコンソールへ表示する検証用サーバー",
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "mock": "node src/index.js --mock",
    "verify": "node src/index.js --mock --duration=10",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/logger.js && node --check src/events.js && node --check src/printer.js && node --check src/sources/tiktok.js && node --check src/sources/mock.js && node --check src/doctor.js",
    "doctor": "node src/doctor.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jisjtb-ui/tikhub.git"
  },
  "license": "MIT",
  "dependencies": {
    "tiktok-live-connector": "^2.4.4"
  }
}
